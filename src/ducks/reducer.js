//INITIAL STATE
const initialState = {};

//ACTION TYPES

//ACTION CREATORS

//REDUCER
export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
